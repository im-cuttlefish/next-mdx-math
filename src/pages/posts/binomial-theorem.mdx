import { Layout } from "../../components/Layout";
import { Math as $ } from "../../components/Math";
import { Theorem } from "../../components/theorem";

export default Layout;

## 二項定理の証明

一般に、次のことが知られている。

<Theorem name="二項定理">
  <$>n</$>を自然数とする。
  <$ display>
    {"(a + b)^n = \\sum_{k = 0}^n {}_nC_k a^k b^{n - k}"}
  </$>
</Theorem>

これを二項定理という。
